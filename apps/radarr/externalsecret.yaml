apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-api-token
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: radarr-api-token
    creationPolicy: Owner
  data:
    - secretKey: API_KEY
      remoteRef:
        key: secret/data/radarr
        property: API_KEY